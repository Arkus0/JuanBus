name: Debug Surbus Fetch

on:
  push:
    branches:
      - claude/surbus-stops-update-zzgd25

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Fetch full surbus.es snapshot
        run: node scripts/_tmp-fetch-surbus-snapshot.mjs
